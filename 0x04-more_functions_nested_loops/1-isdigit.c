#include "main.h"
/**
 * _isdigit - checks for digit
 * @c: digit
 *
 * Return: 1 if true  or 0 if false anyelse
 */
int _isdigit(int c)
{
	for (c = 0; c < 9; c++)
	{
		return (1);
	}
		return (0);
}
